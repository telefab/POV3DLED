This is a library to control a LED globe using low-level functions. It also defines a bus that can be used to communicate with the globe using its ground wire.

Examples are available to display the world, to display a runner trying to go faster than the globe, or to display text.

For bus communication, an example is available where the text and the color can be changed.

